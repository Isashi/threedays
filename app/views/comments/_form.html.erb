<h3>Post a Comment</h3>

<%= form_for [ @place, @comment ] do |f| %>
  <p>
    <%= current_user.username %>
  </p>
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
